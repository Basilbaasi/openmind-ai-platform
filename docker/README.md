# Docker

This directory contains environment-specific Docker configurations.

Future additions:
- `Dockerfile.dev` – Development image with debug tools
- `Dockerfile.test` – Test runner image
- `nginx.conf` – Reverse proxy configuration
