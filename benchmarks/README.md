# Benchmarks

This directory will contain performance benchmarks for:

- API response latency (p50, p95, p99)
- Throughput under concurrent load
- LLM inference time (future phases)
- Memory and CPU profiling results

Tools to consider: `locust`, `wrk`, `pytest-benchmark`.
