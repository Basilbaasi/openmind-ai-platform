"""
Services package.

Contains business-logic services. Each service encapsulates a domain
capability (e.g., LLM orchestration, conversation management, prompt
building) and is injected into route handlers via FastAPI's Depends().
"""
